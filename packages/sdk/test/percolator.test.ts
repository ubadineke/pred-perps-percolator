import assert from"node:assert/strict";import test from"node:test";import{buildClosePositionData,buildCreatePortfolioData,buildDepositData,buildHardFlatData,buildPermissionlessCrankData,buildTradeCpiData,quoteFromMatcher}from"../src/percolator.ts";
test("builds stable Percolator wire payloads",()=>{assert.deepEqual([...buildCreatePortfolioData()],[1]);assert.equal(buildDepositData(1n,2n,3n).length,33);const t=buildTradeCpiData({traderPortfolioId:1n,traderPositionEpoch:2n,lpPortfolioId:3n,lpPositionEpoch:4n,lpMatcherSequence:5n,assetIndex:2,marketId:7n,sizeQ:-1n,feeBps:30n,limitPriceE6:500000n});assert.equal(t[0],10);assert.equal(t.length,100);assert.equal(buildPermissionlessCrankData(0n,[1,2]).length,16)});
test("quotes and closes through the same matcher wire path",()=>{assert.equal(quoteFromMatcher({bidE6:490000n,askE6:510000n,side:"long",feeBps:2n,expiresAtSlot:9n,matcherSequence:3n}).priceE6,510000n);const common={traderPortfolioId:1n,traderPositionEpoch:2n,lpPortfolioId:3n,lpPositionEpoch:4n,lpMatcherSequence:5n,assetIndex:2,marketId:7n,feeBps:30n,limitPriceE6:500000n};assert.equal(buildClosePositionData({...common,openSizeQ:8n})[0],10);assert.equal(buildHardFlatData(1n,2n,3n,4n,1,2n,3n).length,67)});
