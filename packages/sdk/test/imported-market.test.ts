import assert from "node:assert/strict";import test from "node:test";import{encodeActivateImportedPerp,encodeObservation,hashIdentity}from"../src/imported-market.ts";
const i={externalMarketId:"market-42",externalYesId:"yes",externalNoId:"no",title:"Will it ship?",rules:"Official result only",externalCloseTime:2_000_000_000n,assetIndex:1,marketId:7n};
test("activation freezes the full source identity",()=>{const b=encodeActivateImportedPerp(i,550_000n,99n);assert.equal(b.length,195);assert.equal(Buffer.from(b.slice(1,33)).toString("hex"),Buffer.from(hashIdentity("market-42")).toString("hex"));assert.equal(new DataView(b.buffer).getBigUint64(171,true),7n)});
test("observations bind rules, generation, and sequence",()=>{const b=encodeObservation(i,560_000n,1_800_000_000n,2n);assert.equal(b.length,99);assert.equal(new DataView(b.buffer).getBigUint64(67,true),7n);assert.equal(new DataView(b.buffer).getBigUint64(91,true),2n);assert.throws(()=>encodeObservation(i,1_000_000n,1n,2n),/probability/)});
