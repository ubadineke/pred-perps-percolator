export type Market={slug:string;address:string;category:string;question:string;short:string;provider:string;moxie:number;index:number;mark:number;change:number|null;volume:string;oi:string;lock:string;leverage:string;status:number;marketId:string;assetIndex:number;oracleUpdatedAt:string};
export type ApiMarket={address:string;providerMarketId:string;title:string;rules:string;slot:number;assetIndex:number;marketId:string;status:number;markE6:string;indexE6:string;closeTime:string;oracleUpdatedAt:string};
const cents=(e6:string)=>Number(e6)/10_000;
const lockLabel=(seconds:string)=>{const remaining=Number(seconds)-Math.floor(Date.now()/1000);if(!Number.isFinite(remaining))return"—";if(remaining<=0)return"Locked";const days=Math.floor(remaining/86400),hours=Math.floor(remaining%86400/3600);return days?`${days}d ${hours}h`:`${hours}h`};
export function toMarket(x:ApiMarket):Market{const title=x.title.trim()||`Imported prediction market #${x.marketId}`;return{slug:x.address,address:x.address,category:"EVENT",question:title,short:`MARKET ${x.marketId}`,provider:"Imported",moxie:cents(x.markE6),index:cents(x.indexE6),mark:cents(x.markE6),change:null,volume:"—",oi:"—",lock:lockLabel(x.closeTime),leverage:"5×",status:x.status,marketId:x.marketId,assetIndex:x.assetIndex,oracleUpdatedAt:x.oracleUpdatedAt}}
