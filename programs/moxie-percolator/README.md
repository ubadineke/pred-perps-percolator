# Moxie Percolator wrapper

Phase 1 uses the pinned upstream wrapper in `vendor/percolator-prog`. Phase 6 will establish the
minimal Moxie fork here, retaining Percolator's instruction ABI unless a documented policy change
requires otherwise.

